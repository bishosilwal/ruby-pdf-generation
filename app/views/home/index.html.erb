
	<h3>User's Home</h3>
	<%if user_signed_in?%>
			<%if @user_documents%>
				<div class="">
						<table class="table table-bordered table-hover ">
							<thead>
								<tr>
									<th>Documents name <i class="fa fa-file-pdf-o fa-lg" aria-hidden="true"></i></th>
									<th>File size(Kb)</th>
									<th>Action</th>
								</tr>
							</thead>
							<tbody>
								<%@user_documents.each do |document|%>
								<tr>
									<td><%=document.document_file_name%></td>
									<td><%=document.document_file_size%></td>
									<td>
											<div class="btn btn-outline-danger">
												<%= link_to "Delete",home_path(document.id),method: :delete,data:{confirm: "Are you sure want to delete this file?"},style: "text-decoration: none; color: black;"%>
												<i class="fa fa-trash-o" style="color: black;"></i>
										</div>
												
										<button type="button" class="btn btn-primary" data-id=<%=document.id%> data-toggle="modal" data-target="#viewPdf">View
										  <i class="fa fa-eye " alt="test view"></i>
										</button>
										<button type="button" class="btn btn-success" data-id=<%=document.id%> data-toggle="modal" data-target="#sharePdf">
											<i class="fa fa-share"></i>Share				  
										</button>
							
									</td>
								</tr>
								<%end%>
							</tbody>
						</table>
				</div>
				<%=paginate @user_documents,param_name: :user_documents%>
				<%end%>
		<%else%>
			<h3>Please signed in to create document or view your documents</h3>
		<%end%>
		
</div>
<%=render "shared/viewpdf" %>
<%=render "shared/sharepdf" %>

